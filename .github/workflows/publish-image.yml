name: publish-image

on:
  push:
    tags: ['v[0-9]+.[0-9]+.[0-9]+']
  workflow_dispatch:

permissions:
  contents: read

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository }}
  COMPONENTS: db auth kong storage converter api mcp web

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 150
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Free disk space
        run: |
          df -h /
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/.ghcup /opt/hostedtoolcache/CodeQL /usr/local/share/boost
          sudo docker image prune -af
          df -h /

      - name: Static checks
        run: tests/static.sh

      - name: Resolve wrapper version
        id: ver
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then v="${GITHUB_REF#refs/tags/v}"; else v="0.0.0-${GITHUB_SHA::7}"; fi
          echo "version=$v" >> "$GITHUB_OUTPUT"

      # Every wrapper is built once, as a local candidate, and the whole bundle is tested with those
      # exact images. The images pushed afterwards are the tested ones, retagged, not a rebuild.
      # amd64 only: the apps are built from source here and Railway runs amd64.
      - name: Build candidates
        env:
          WRAPPER_VERSION: ${{ steps.ver.outputs.version }}
          VCS_REF: ${{ github.sha }}
          LLMWIKI_RAILWAY_DB_IMAGE: llmwiki-railway-db:candidate
          LLMWIKI_RAILWAY_AUTH_IMAGE: llmwiki-railway-auth:candidate
          LLMWIKI_RAILWAY_KONG_IMAGE: llmwiki-railway-kong:candidate
          LLMWIKI_RAILWAY_STORAGE_IMAGE: llmwiki-railway-storage:candidate
          LLMWIKI_RAILWAY_CONVERTER_IMAGE: llmwiki-railway-converter:candidate
          LLMWIKI_RAILWAY_API_IMAGE: llmwiki-railway-api:candidate
          LLMWIKI_RAILWAY_MCP_IMAGE: llmwiki-railway-mcp:candidate
          LLMWIKI_RAILWAY_WEB_IMAGE: llmwiki-railway-web:candidate
        run: docker compose build --pull

      - name: Smoke tests against candidates
        env:
          LLMWIKI_RAILWAY_DB_IMAGE: llmwiki-railway-db:candidate
          LLMWIKI_RAILWAY_AUTH_IMAGE: llmwiki-railway-auth:candidate
          LLMWIKI_RAILWAY_KONG_IMAGE: llmwiki-railway-kong:candidate
          LLMWIKI_RAILWAY_STORAGE_IMAGE: llmwiki-railway-storage:candidate
          LLMWIKI_RAILWAY_CONVERTER_IMAGE: llmwiki-railway-converter:candidate
          LLMWIKI_RAILWAY_API_IMAGE: llmwiki-railway-api:candidate
          LLMWIKI_RAILWAY_MCP_IMAGE: llmwiki-railway-mcp:candidate
          LLMWIKI_RAILWAY_WEB_IMAGE: llmwiki-railway-web:candidate
        run: tests/smoke.sh

      - name: Persistence tests against candidates
        env:
          LLMWIKI_RAILWAY_DB_IMAGE: llmwiki-railway-db:candidate
          LLMWIKI_RAILWAY_AUTH_IMAGE: llmwiki-railway-auth:candidate
          LLMWIKI_RAILWAY_KONG_IMAGE: llmwiki-railway-kong:candidate
          LLMWIKI_RAILWAY_STORAGE_IMAGE: llmwiki-railway-storage:candidate
          LLMWIKI_RAILWAY_CONVERTER_IMAGE: llmwiki-railway-converter:candidate
          LLMWIKI_RAILWAY_API_IMAGE: llmwiki-railway-api:candidate
          LLMWIKI_RAILWAY_MCP_IMAGE: llmwiki-railway-mcp:candidate
          LLMWIKI_RAILWAY_WEB_IMAGE: llmwiki-railway-web:candidate
        run: tests/persistence.sh

      - name: Container logs on failure
        if: failure()
        run: docker compose logs --no-color --tail 200 || true

      - name: Tear down test stack
        if: always()
        run: docker compose down -v --remove-orphans || true

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push the tested images
        env:
          VERSION: ${{ steps.ver.outputs.version }}
        run: |
          set -euo pipefail
          tags=("$VERSION")
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then tags+=("${VERSION%.*}" latest); fi
          {
            echo "### Published images"
            echo
            echo "| Component | Reference |"
            echo "|---|---|"
          } >> "$GITHUB_STEP_SUMMARY"
          for c in $COMPONENTS; do
            for t in "${tags[@]}"; do
              docker tag "llmwiki-railway-$c:candidate" "$IMAGE_PREFIX-$c:$t"
              docker push -q "$IMAGE_PREFIX-$c:$t"
            done
            digest=$(docker image inspect --format '{{index .RepoDigests 0}}' "$IMAGE_PREFIX-$c:$VERSION")
            echo "| $c | \`$digest\` |" >> "$GITHUB_STEP_SUMMARY"
          done
